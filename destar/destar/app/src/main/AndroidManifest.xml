<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.destar.platform.destar">

    <supports-screens
        android:largeScreens="true"
        android:normalScreens="true"
        android:anyDensity="true"
        android:resizeable="true"
        android:smallScreens="true"
        android:xlargeScreens="true" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_INTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_INTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".AppClass"
        android:allowBackup="true"
        android:icon="@drawable/logo_destar"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_destar"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_api_key" />

        <service android:name=".service.MyFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <service android:name=".service.MyFirebaseInstanceIDService">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>
        <service
            android:name=".service.MyLocationService"
            android:process=":mylocation_service">
            <intent-filter>
                <action android:name="LOKASI" />
            </intent-filter>
        </service>

        <activity
            android:name=".SplashActivity"
            android:label="DESTAR"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DeantarMapsActivity"
            android:parentActivityName=".TransportasiActivity"
            android:value="AIzaSyBd0ivEYNo5TNR5R18gxte8YmbnU8mIRYE">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".TransportasiActivity" />
        </activity>
        <activity
         android:name=".deSewaActivity"
         android:parentActivityName=".Dashboard">
         <meta-data
         android:name="android.support.PARENT_ACTIVITY"
         android:value=".Dashboard" />
         </activity>
        <activity android:name=".TampilanAwalActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MapsActivity" />
        <activity android:name=".Toolbar" />
        <activity android:name=".PesananActivity" />
        <activity android:name=".RiwayatActivity" />
        <activity android:name=".AkunActivity" />
        <activity android:name=".Dashboard" />
        <activity
            android:name=".TransportasiActivity"
            android:parentActivityName=".Dashboard">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".Dashboard" />
        </activity>
        <activity android:name=".NextPengantarBarang" />
        <activity android:name=".deSewaLainnya" />
        <activity android:name=".AlatMedis" />
        <activity android:name=".AlatPertanian" />
        <activity android:name=".Kendaraan" />
        <activity android:name=".NextDesewa"></activity>
        <activity android:name=".pop_up"/>
    </application>

</manifest>